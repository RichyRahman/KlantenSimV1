<Window x:Class="KlantenSimulatorUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KlantenSimulatorUI"
        mc:Ignorable="d"
        Title="Klanten Simulator" Height="350" Width="500">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Selecteer een land:" 
                   FontSize="14"
                   FontWeight="Bold"/>

        <ComboBox x:Name="CountryComboBox"
                  Grid.Row="1"
                  Margin="0,10,0,10"
                  Height="30"/>

        <StackPanel Grid.Row="2">
            <Button Content="Genereer klant"
                    Height="35"
                    Margin="0,0,0,10"
                    Click="GenerateCustomer_Click"/>

            <TextBox x:Name="ResultTextBox"
                     IsReadOnly="True"
                     FontSize="14"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"/>
        </StackPanel>
    </Grid>
</Window>
